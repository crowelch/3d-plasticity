 <ul>
	{{#each users as |user|}}
		<li>{{user.name}}</li>
		<li>{{user.location}}</li>
		<li>{{user.printer}}</li>
		{{#if user.printer.printerResolutions}}
			<ul>
				{{#each user.printer.printerResolutions as |resolution|}}
					<li>{{resolution}}</li>
				{{/each}}
			</ul
		{{/if}}
		<li>{{user.price}}</li>
		<br>
	{{/each}}
</ul>
